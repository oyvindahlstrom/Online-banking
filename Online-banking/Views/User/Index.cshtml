
@{
    ViewBag.Title = "Index";
}

<link href="~/StyleSheet/index.css" rel="stylesheet" />

<div class="row">
    <div class="col-md-12">
        <div class="col-md-offset-4 col-md-4">
            @Html.Label("LOGIN", new { @class = "login-label" })

            <a href="@Url.Action("LogIn", "User")" class="btn btn-lg btn-block btn-default">
                ... with BankID &nbsp;
                <span class="glyphicon glyphicon-lock"></span>
            </a>

        </div>
        <div class="col-md-offset-4 col-md-4">
            @Html.ActionLink("Run Testdata", "Test_Data", "Bank", new { empty = "" }, new { @class = "btn btn-lg btn-block btn-primary" })
        </div>
    </div>
</div>
